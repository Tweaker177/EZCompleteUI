<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for UIDocumentPickerViewController to present external file access -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Required for presenting view controllers from external processes (file picker, QuickLook) -->
    <key>com.apple.UIKit.vends-view-services</key>
    <true/>

    <!-- Rootless jailbreak (Dopamine/palera1n): bypass sandbox restrictions -->
    <!-- Without this the document picker silently fails to return files on iOS 15 -->
    <key>com.apple.private.security.no-sandbox</key>
    <true/>

    <!-- Keep container accessible without strict enforcement -->
    <key>com.apple.private.security.container-required</key>
    <false/>

    <!-- Application groups — empty array satisfies entitlement checks -->
    <key>com.apple.security.application-groups</key>
    <array/>
</dict>
</plist>
